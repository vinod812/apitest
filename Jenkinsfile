pipeline{
	agent any
	stages{
			stage("Verify Branch")
			{
				steps {
					echo "$GIT_BRANCH"
				}
			}
			
			stage("Run the Test")
			{
				steps {
					echo "$GIT_BRANCH"
				}
			}
			
			
	}
}
